# Metabase API credentials (LOCAL ONLY; do not commit secrets)
MB_HOST=http://127.0.0.1:3000
MB_USER=your-admin@example.com
MB_PASS=replace_me

# Either set MB_DB_ID or MB_DB_NAME (the script resolves by name if present)
# MB_DB_ID=123
MB_DB_NAME=AI Usage Analytics (BigQuery)

# Optional: place dashboards under a collection (numeric id); leave empty for root
# MB_COLLECTION_ID=1
